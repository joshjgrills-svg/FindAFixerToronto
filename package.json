
{
  "name": "findafixer-toronto",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@google/genai": "^1.34.0"
  },
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step required for ESM projects' && exit 0"
  }
}
